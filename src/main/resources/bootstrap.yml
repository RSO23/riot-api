spring:
  application:
    name: riot-api
  cloud:
    consul:
      host: localhost
      config:
        fail-fast: false
        format: yaml
      discovery:
        fail-fast: false
        prefer-ip-address: true

---
spring:
  profiles: prod
  cloud:
    consul:
      host: consul-consul-server